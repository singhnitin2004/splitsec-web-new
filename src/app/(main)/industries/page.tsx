import IndustriesPage from "@/components/pages/Industries";

export const metadata = {
  title: "Industries | SplitSec.AI",
  description: "Gunshot detection you can deploy in minutes. No cameras, no wiring. Clear setup and pilot plans for outdoor events, guard companies, health care, and schools.",
};

export default function IndustriesRoute() {
  return <IndustriesPage />;
}
